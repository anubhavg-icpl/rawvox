# Real-time ASR - uncensored, no content filtering
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
webrtcvad>=2.0.10

# Deep web search - uncensored multi-engine
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
trafilatura>=1.6.0

# Content processing
sentence-transformers>=2.2.0
torch>=2.0.0

# TTS output - multiple backends (auto-detected, best first)
kokoro>=0.9.0            # #1 HF TTS (9.7M downloads) - af_bella, af_heart
misaki>=0.9.0            # Kokoro G2P engine
en-core-web-sm>=3.8.0    # Spacy model required by Kokoro
edge-tts>=7.0.0          # Microsoft neural voices (fallback)
soundfile>=0.12.0        # Audio file I/O
TTS>=0.22.0              # Optional: Coqui XTTS-v2 (GPU required, voice cloning)

# API & CLI
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
rich>=13.0.0
click>=8.1.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
aiofiles>=23.0.0

# =========================================================
# RawVox runtime deps
# =========================================================

# --- ASR (uncensored, no content filter) ---
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0,<2.3
webrtcvad>=2.0.10

# --- HTTP + scraping ---
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Semantic ranking ---
sentence-transformers>=2.2.0
torch>=2.0.0

# --- TTS ---
# Kokoro-82M (#1 HF TTS, 9.7M downloads) + edge-tts fallback.
# The en_core_web_sm spaCy model is downloaded by the Dockerfile
# (or `python -m spacy download en_core_web_sm` locally) — it is
# NOT a regular PyPI package and cannot live in this file.
kokoro>=0.9.0
misaki>=0.9.0
spacy>=3.7.0
edge-tts>=7.0.0
soundfile>=0.12.0

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
websockets>=12.0
python-multipart>=0.0.6

# --- CLI + utils ---
rich>=13.0.0
click>=8.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
